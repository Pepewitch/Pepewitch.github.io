service cloud.firestore {
  match /databases/{database}/documents {
    match /showcase/{showcase} {
      allow write, delete: if request.auth.uid == userId;
    }
    match /{document=**} {
      allow read;
    }
  }
}
